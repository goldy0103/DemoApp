BROKER SCHEMA com.demo


CREATE COMPUTE MODULE DemoMainFlow_Request
	CREATE FUNCTION Main() RETURNS BOOLEAN
	BEGIN
--		SET OutputRoot.MQMD.ReplyToQ = 'REQ.OUT';
--		SET OutputRoot.MQMD.ReplyToQMgr = 'DEMOQM';
		
		SET OutputRoot.BLOB.BLOB = InputRoot.BLOB.BLOB;
		
		SET OutputLocalEnvironment.MQ.GET.MQGMO.Options = MQGMO_CONVERT;
		
		RETURN TRUE;
	END;
END MODULE;
